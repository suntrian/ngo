<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ngo.basic.dao.BaseDao">

    <select id="queryOneBySql" parameterType="string" resultType="map">
        ${value}
    </select>

    <select id="queryListBySql" parameterType="string" resultType="map">
        ${value}
    </select>

    <insert id="insertBySql" parameterType="string">
        ${value}
    </insert>

    <delete id="deleteBySql" parameterType="string">
        ${value}
    </delete>

    <update id="updateBySql" parameterType="string">
        ${value}
    </update>

</mapper>